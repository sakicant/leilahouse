node_modules/
.DS_Store
Thumbs.db
*.log
api/inquiries-failed.log
.vercel/

# logo build artefacts (the generated SVG/PNG are committed, the font is not)
.build/
_preview.html

# Admin credentials: never commit these (this repository is public).
api/config.php
api/admin-attempts.log

# The calendar admin panel ships in the upload zip, not through GitHub.
# It holds no credentials, but there is no reason to publish its address.
manage-*/
dist/
